---
title: pg19
weight: 415
description: PostgreSQL 19 Beta 单节点试用模板，启用 PGDG Beta 仓库并使用最小运行时
icon: fa-solid fa-flask
categories: [参考]
---

`pg19` 是 PostgreSQL 19 Beta 的单节点试用模板。它基于 `meta` 拓扑，但启用 `beta` 软件仓库，并刻意缩小软件包与扩展范围。


--------

## 配置概览

- 配置名称：`pg19`
- 节点数量：单节点
- PostgreSQL 版本：`19` Beta
- 适用场景：新版本功能验证、兼容性测试
- 相关配置：[`meta`](/docs/conf/meta/)、[`pgsql`](/docs/conf/pgsql/)

启用方式：

```bash
./configure -c pg19 [-i <primary_ip>]
```


--------

## 配置内容

源文件地址：[`pigsty/conf/pg19.yml`](https://github.com/pgsty/pigsty/blob/main/conf/pg19.yml)

{{< readfile file="yaml/pg19.yml" code="true" lang="yaml" >}}


--------

## 配置解读

模板的关键限制与默认值：

- `node_repo_modules: node,infra,pgsql,beta`，从 PGDG Beta 仓库获取 PG19 软件包
- `pg_packages` 仅包含内核、Patroni、PgBouncer、Exporter 与 VIP 组件
- `pg_extensions: []`，不安装扩展包
- `pgbackrest_enabled: false`，同时禁用 pgBackRest Exporter 与默认全量备份任务
- 保留 INFRA、ETCD、PGSQL 与可选 pgAdmin 的单节点体验

这是 Beta 试用配置，不是生产模板。不要通过 `-v 19` 把普通模板直接当作 PG19 生产配置；扩展、备份和升级流程需要分别验证。
