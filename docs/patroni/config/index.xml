<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Patroni 配置 on PIGSTY</title><link>https://pigsty.cc/docs/patroni/config/</link><description>Recent content in Patroni 配置 on PIGSTY</description><generator>Hugo</generator><language>zh-CN</language><atom:link href="https://pigsty.cc/docs/patroni/config/index.xml" rel="self" type="application/rss+xml"/><item><title>动态配置</title><link>https://pigsty.cc/docs/patroni/config/dynamic/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pigsty.cc/docs/patroni/config/dynamic/</guid><description>&lt;blockquote&gt;
&lt;p&gt;原始页面： &lt;a href="https://patroni.readthedocs.io/en/latest/dynamic_configuration.html"&gt;https://patroni.readthedocs.io/en/latest/dynamic_configuration.html&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;a id="dynamic"&gt;&lt;/a&gt;
动态配置存储在 DCS（分布式配置存储）中，并应用于集群的所有节点。&lt;/p&gt;
&lt;p&gt;如需修改动态配置，可使用 &lt;a href="https://pigsty.cc/docs/patroni/patronictl#patronictl_edit_config"&gt;&lt;code&gt;patronictl edit-config&lt;/code&gt;&lt;/a&gt; 工具或 Patroni &lt;a href="https://pigsty.cc/docs/patroni/rest_api#rest_api"&gt;&lt;strong&gt;REST API&lt;/strong&gt;&lt;/a&gt;。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;loop_wait&lt;/code&gt;&lt;/strong&gt;：主循环每轮的休眠秒数。默认值：10，最小值：1。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;ttl&lt;/code&gt;&lt;/strong&gt;：获取领导者锁的 TTL（秒）。可以理解为触发自动故障转移前的等待时长。默认值：30，最小值：20。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;retry_timeout&lt;/code&gt;&lt;/strong&gt;：DCS 和 PostgreSQL 操作的重试超时时间（秒）。若 DCS 或网络故障时长短于此值，Patroni 不会对主库执行降级。默认值：10，最小值：3。&lt;/li&gt;
&lt;/ul&gt;
&lt;div class="td-alert td-alert--md alert alert-warning" role="alert"&gt;&lt;div class="td-alert-heading alert-heading" role="heading"&gt;警告&lt;/div&gt;
 &lt;div class="td-alert-body"&gt;
 &lt;p&gt;修改 &lt;strong&gt;&lt;code&gt;loop_wait&lt;/code&gt;&lt;/strong&gt;、&lt;strong&gt;&lt;code&gt;retry_timeout&lt;/code&gt;&lt;/strong&gt; 或 &lt;strong&gt;&lt;code&gt;ttl&lt;/code&gt;&lt;/strong&gt; 时，必须遵守以下约束：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="background-color:#f8f8f8;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"&gt;&lt;code class="language-python" data-lang="python"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;&lt;span style="color:#000"&gt;loop_wait&lt;/span&gt; &lt;span style="color:#ce5c00;font-weight:bold"&gt;+&lt;/span&gt; &lt;span style="color:#0000cf;font-weight:bold"&gt;2&lt;/span&gt; &lt;span style="color:#ce5c00;font-weight:bold"&gt;*&lt;/span&gt; &lt;span style="color:#000"&gt;retry_timeout&lt;/span&gt; &lt;span style="color:#ce5c00;font-weight:bold"&gt;&amp;lt;=&lt;/span&gt; &lt;span style="color:#000"&gt;ttl&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;
 &lt;/div&gt;
&lt;/div&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;maximum_lag_on_failover&lt;/code&gt;&lt;/strong&gt;：从库参与主库竞选所允许的最大落后字节数。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;maximum_lag_on_syncnode&lt;/code&gt;&lt;/strong&gt;：同步从库在被健康的异步从库替代之前，允许落后的最大字节数。若存在多个从库，Patroni 取所有从库中最大的 LSN 作为参考；若只有一个从库，则使用主库当前的 WAL LSN。默认值为 -1；当该值为 0 或更低时，Patroni 不主动替换不健康的同步从库。请将此值设置得足够高，避免在高事务量期间 Patroni 频繁替换同步从库。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;max_timelines_history&lt;/code&gt;&lt;/strong&gt;：DCS 中保留的时间线历史条目最大数量。默认值：0。设置为 0 时，保留完整历史记录。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;primary_start_timeout&lt;/code&gt;&lt;/strong&gt;：主库在触发故障转移之前，允许从故障中恢复的时间（秒）。默认值为 300 秒。设置为 0 时，一旦检测到崩溃就立即执行故障转移（如果可能）。使用异步复制时，故障转移可能导致事务丢失。主库故障时的最长故障转移时间为 &lt;strong&gt;&lt;code&gt;loop_wait + primary_start_timeout + loop_wait&lt;/code&gt;&lt;/strong&gt;；若 &lt;strong&gt;&lt;code&gt;primary_start_timeout&lt;/code&gt;&lt;/strong&gt; 为零，则仅为 &lt;strong&gt;&lt;code&gt;loop_wait&lt;/code&gt;&lt;/strong&gt;。请根据持久性与可用性的取舍来设置该值。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;primary_stop_timeout&lt;/code&gt;&lt;/strong&gt;：Patroni 停止 PostgreSQL 时允许等待的秒数，仅在启用 &lt;strong&gt;&lt;code&gt;synchronous_mode&lt;/code&gt;&lt;/strong&gt; 时生效。当该值大于 0 且启用了同步模式时，若停止操作运行时间超过 &lt;strong&gt;&lt;code&gt;primary_stop_timeout&lt;/code&gt;&lt;/strong&gt; 所设定的值，Patroni 会向 postmaster 发送 SIGKILL 信号。请根据持久性与可用性的取舍来设置该值。若该参数未设置或设置为 &amp;lt;= 0，则不生效。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;synchronous_mode&lt;/code&gt;&lt;/strong&gt;：启用同步复制模式。可选值：&lt;strong&gt;&lt;code&gt;off&lt;/code&gt;&lt;/strong&gt;、&lt;strong&gt;&lt;code&gt;on&lt;/code&gt;&lt;/strong&gt;、&lt;strong&gt;&lt;code&gt;quorum&lt;/code&gt;&lt;/strong&gt;。在此模式下，主库负责管理 &lt;strong&gt;&lt;code&gt;synchronous_standby_names&lt;/code&gt;&lt;/strong&gt;，且只有最后已知的领导者或同步从库之一才被允许参与主库竞选。同步模式确保已成功提交的事务在故障转移时不会丢失，代价是当 Patroni 无法保证事务持久性时，写入操作将不可用。详见 &lt;a href="https://pigsty.cc/docs/patroni/replication_modes#replication_modes"&gt;&lt;strong&gt;复制模式文档&lt;/strong&gt;&lt;/a&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;synchronous_mode_strict&lt;/code&gt;&lt;/strong&gt;：当没有可用的同步从库时，禁止关闭同步复制，阻塞所有客户端对主库的写入。详见 &lt;a href="https://pigsty.cc/docs/patroni/replication_modes#replication_modes"&gt;&lt;strong&gt;复制模式文档&lt;/strong&gt;&lt;/a&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;synchronous_node_count&lt;/code&gt;&lt;/strong&gt;：启用 &lt;a href="https://pigsty.cc/docs/patroni/replication_modes#synchronous_mode"&gt;&lt;code&gt;synchronous_mode&lt;/code&gt;&lt;/a&gt; 时，Patroni 使用此参数精确控制同步备库实例的数量，并随成员的加入和离开动态调整 DCS 中的状态及 PostgreSQL 的 &lt;strong&gt;&lt;code&gt;synchronous_standby_names&lt;/code&gt;&lt;/strong&gt; 参数。若设置值高于符合条件的节点数，将被自动调低。默认值为 &lt;strong&gt;&lt;code&gt;1&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;failsafe_mode&lt;/code&gt;&lt;/strong&gt;：启用 &lt;a href="https://pigsty.cc/docs/patroni/dcs_failsafe_mode#dcs_failsafe_mode"&gt;&lt;strong&gt;DCS 故障安全模式&lt;/strong&gt;&lt;/a&gt;。默认值为 &lt;strong&gt;&lt;code&gt;false&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;postgresql&lt;/code&gt;&lt;/strong&gt;：
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;use_pg_rewind&lt;/code&gt;&lt;/strong&gt;：是否使用 pg_rewind。默认值为 &lt;strong&gt;&lt;code&gt;false&lt;/code&gt;&lt;/strong&gt;。注意：集群必须以数据页校验和（&lt;strong&gt;&lt;code&gt;initdb&lt;/code&gt;&lt;/strong&gt; 的 &lt;strong&gt;&lt;code&gt;--data-checksums&lt;/code&gt;&lt;/strong&gt; 选项）初始化，且/或将 &lt;strong&gt;&lt;code&gt;wal_log_hints&lt;/code&gt;&lt;/strong&gt; 设置为 &lt;strong&gt;&lt;code&gt;on&lt;/code&gt;&lt;/strong&gt;，否则 &lt;strong&gt;&lt;code&gt;pg_rewind&lt;/code&gt;&lt;/strong&gt; 将无法工作。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;use_slots&lt;/code&gt;&lt;/strong&gt;：是否使用复制槽。在 PostgreSQL 9.4+ 上默认值为 &lt;strong&gt;&lt;code&gt;true&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;recovery_conf&lt;/code&gt;&lt;/strong&gt;：配置从库时写入 &lt;strong&gt;&lt;code&gt;recovery.conf&lt;/code&gt;&lt;/strong&gt; 的附加配置项。PostgreSQL 12 已不再使用 &lt;strong&gt;&lt;code&gt;recovery.conf&lt;/code&gt;&lt;/strong&gt;，但仍可继续使用此节，Patroni 会透明地处理。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;parameters&lt;/code&gt;&lt;/strong&gt;：PostgreSQL 配置参数（GUC），格式为 &lt;strong&gt;&lt;code&gt;{max_connections: 100, wal_level: &amp;quot;replica&amp;quot;, max_wal_senders: 10, wal_log_hints: &amp;quot;on&amp;quot;}&lt;/code&gt;&lt;/strong&gt;。其中许多参数是复制正常工作的必要条件。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;pg_hba&lt;/code&gt;&lt;/strong&gt;：Patroni 用于生成 &lt;strong&gt;&lt;code&gt;pg_hba.conf&lt;/code&gt;&lt;/strong&gt; 的规则列表。若 PostgreSQL 参数 &lt;strong&gt;&lt;code&gt;hba_file&lt;/code&gt;&lt;/strong&gt; 被设置为非默认值，Patroni 将忽略此参数。
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;- host all all 0.0.0.0/0 md5&lt;/code&gt;&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;- host replication replicator 127.0.0.1/32 md5&lt;/code&gt;&lt;/strong&gt;：复制所必需的一行配置。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;pg_ident&lt;/code&gt;&lt;/strong&gt;：Patroni 用于生成 &lt;strong&gt;&lt;code&gt;pg_ident.conf&lt;/code&gt;&lt;/strong&gt; 的规则列表。若 PostgreSQL 参数 &lt;strong&gt;&lt;code&gt;ident_file&lt;/code&gt;&lt;/strong&gt; 被设置为非默认值，Patroni 将忽略此参数。
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;- mapname1 systemname1 pguser1&lt;/code&gt;&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;- mapname1 systemname2 pguser2&lt;/code&gt;&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;standby_cluster&lt;/code&gt;&lt;/strong&gt;：若定义了此节，表示要引导一个备用集群。
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;host&lt;/code&gt;&lt;/strong&gt;：远端节点的地址。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;port&lt;/code&gt;&lt;/strong&gt;：远端节点的端口。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;primary_slot_name&lt;/code&gt;&lt;/strong&gt;：用于复制的远端节点槽名。此参数可选，默认值由实例名称派生（参见函数 &lt;strong&gt;&lt;code&gt;slot_name_from_member_name&lt;/code&gt;&lt;/strong&gt;）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;create_replica_methods&lt;/code&gt;&lt;/strong&gt;：从远端主库引导备用领导者所用方法的有序列表，可与 &lt;a href="https://pigsty.cc/docs/patroni/config/yaml#postgresql_settings"&gt;&lt;code&gt;postgresql_settings&lt;/code&gt;&lt;/a&gt; 中定义的列表不同。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;restore_command&lt;/code&gt;&lt;/strong&gt;：将 WAL 记录从远端主库恢复到备用集群节点的命令，可与 &lt;a href="https://pigsty.cc/docs/patroni/config/yaml#postgresql_settings"&gt;&lt;code&gt;postgresql_settings&lt;/code&gt;&lt;/a&gt; 中定义的不同。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;archive_cleanup_command&lt;/code&gt;&lt;/strong&gt;：备用领导者的归档清理命令。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;recovery_min_apply_delay&lt;/code&gt;&lt;/strong&gt;：备用领导者实际应用 WAL 记录前的等待时长。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;member_slots_ttl&lt;/code&gt;&lt;/strong&gt;：从库关闭后，其物理复制槽的保留时间。默认值：&lt;strong&gt;&lt;code&gt;30min&lt;/code&gt;&lt;/strong&gt;。若希望保留旧行为（成员键从 DCS 过期后立即删除槽），可将其设置为 &lt;strong&gt;&lt;code&gt;0&lt;/code&gt;&lt;/strong&gt;。此功能仅在 PostgreSQL 11 及以上版本中有效。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;slots&lt;/code&gt;&lt;/strong&gt;：定义永久复制槽。这些复制槽在主从切换/故障转移期间会被保留，不存在的永久槽由 Patroni 负责创建。从 PostgreSQL 11 起，永久物理槽在所有节点上创建，其位置每 &lt;strong&gt;&lt;code&gt;loop_wait&lt;/code&gt;&lt;/strong&gt; 秒推进一次。对于 PostgreSQL 11 以下版本，永久物理复制槽仅在当前主库上维护。逻辑槽通过重启从主库复制到从库，此后其位置每 &lt;strong&gt;&lt;code&gt;loop_wait&lt;/code&gt;&lt;/strong&gt; 秒推进一次（如有必要）。逻辑槽文件的复制通过 &lt;strong&gt;&lt;code&gt;libpq&lt;/code&gt;&lt;/strong&gt; 连接完成，使用 rewind 或超级用户凭据（参见 &lt;strong&gt;&lt;code&gt;postgresql.authentication&lt;/code&gt;&lt;/strong&gt; 节）。从库上的逻辑槽位置可能略落后于原主库，因此应用程序应做好在故障转移后接收到重复消息的准备，最简便的处理方式是追踪 &lt;strong&gt;&lt;code&gt;confirmed_flush_lsn&lt;/code&gt;&lt;/strong&gt;。启用永久复制槽需要将 &lt;strong&gt;&lt;code&gt;postgresql.use_slots&lt;/code&gt;&lt;/strong&gt; 设置为 &lt;strong&gt;&lt;code&gt;true&lt;/code&gt;&lt;/strong&gt;。若定义了永久逻辑复制槽，Patroni 将自动启用 &lt;strong&gt;&lt;code&gt;hot_standby_feedback&lt;/code&gt;&lt;/strong&gt;。由于逻辑复制槽的故障转移在 PostgreSQL 9.6 及更低版本上不安全，且 PostgreSQL 10 缺少一些重要函数，该功能仅支持 PostgreSQL 11+。
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;my_slot_name&lt;/code&gt;&lt;/strong&gt;：永久复制槽的名称。若槽名称与当前节点名称相同，该槽不会在此节点上创建。若添加的永久物理复制槽名称与某个 Patroni 成员名称相同，Patroni 将确保该槽不被删除，即使对应成员变得无响应（正常情况下这会导致 Patroni 删除该槽）。这在某些场景下很有用，例如希望成员所用的复制槽在临时故障期间持续存在，或将现有成员导入新的 Patroni 集群（详见 &lt;a href="https://pigsty.cc/docs/patroni/existing_data#existing_data"&gt;&lt;strong&gt;将独立实例转换为 Patroni 集群&lt;/strong&gt;&lt;/a&gt;）。但运维人员应注意，当该槽不再需要时，须及时清除 DCS 中的名称冲突配置，否则会影响 Patroni 的正常运行。
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;type&lt;/code&gt;&lt;/strong&gt;：槽类型，可为 &lt;strong&gt;&lt;code&gt;physical&lt;/code&gt;&lt;/strong&gt; 或 &lt;strong&gt;&lt;code&gt;logical&lt;/code&gt;&lt;/strong&gt;。逻辑槽需额外定义 &lt;strong&gt;&lt;code&gt;database&lt;/code&gt;&lt;/strong&gt; 和 &lt;strong&gt;&lt;code&gt;plugin&lt;/code&gt;&lt;/strong&gt;；物理槽可选择定义 &lt;strong&gt;&lt;code&gt;cluster_type&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;database&lt;/code&gt;&lt;/strong&gt;：逻辑槽应创建所在的数据库名称。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;plugin&lt;/code&gt;&lt;/strong&gt;：逻辑槽使用的插件名称。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;cluster_type&lt;/code&gt;&lt;/strong&gt;：槽仅应创建于其上的集群类型（&lt;strong&gt;&lt;code&gt;primary&lt;/code&gt;&lt;/strong&gt; 或 &lt;strong&gt;&lt;code&gt;standby&lt;/code&gt;&lt;/strong&gt;），否则该槽不会被创建，或已存在的槽将被删除。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;ignore_slots&lt;/code&gt;&lt;/strong&gt;：需要 Patroni 忽略的复制槽属性集合列表。当某些复制槽由 Patroni 以外的工具管理时，此配置非常有用。任何属性子集匹配都会导致该槽被忽略。
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;name&lt;/code&gt;&lt;/strong&gt;：复制槽的名称。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;type&lt;/code&gt;&lt;/strong&gt;：槽类型，可为 &lt;strong&gt;&lt;code&gt;physical&lt;/code&gt;&lt;/strong&gt; 或 &lt;strong&gt;&lt;code&gt;logical&lt;/code&gt;&lt;/strong&gt;。逻辑槽可额外定义 &lt;strong&gt;&lt;code&gt;database&lt;/code&gt;&lt;/strong&gt; 和/或 &lt;strong&gt;&lt;code&gt;plugin&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;database&lt;/code&gt;&lt;/strong&gt;：数据库名称（匹配 &lt;strong&gt;&lt;code&gt;logical&lt;/code&gt;&lt;/strong&gt; 槽时使用）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;plugin&lt;/code&gt;&lt;/strong&gt;：逻辑解码插件（匹配 &lt;strong&gt;&lt;code&gt;logical&lt;/code&gt;&lt;/strong&gt; 槽时使用）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;注意：&lt;strong&gt;&lt;code&gt;slots&lt;/code&gt;&lt;/strong&gt; 是哈希映射，而 &lt;strong&gt;&lt;code&gt;ignore_slots&lt;/code&gt;&lt;/strong&gt; 是数组。示例如下：&lt;/p&gt;</description></item><item><title>YAML 配置参数</title><link>https://pigsty.cc/docs/patroni/config/yaml/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pigsty.cc/docs/patroni/config/yaml/</guid><description>&lt;blockquote&gt;
&lt;p&gt;原始页面： &lt;a href="https://patroni.readthedocs.io/en/latest/yaml_configuration.html"&gt;https://patroni.readthedocs.io/en/latest/yaml_configuration.html&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;a id="yaml"&gt;&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="全局通用"&gt;全局/通用&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;name&lt;/code&gt;&lt;/strong&gt;：主机名称，在集群中必须唯一。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;namespace&lt;/code&gt;&lt;/strong&gt;：Patroni 在配置存储中保存集群信息的路径，默认值为 &lt;strong&gt;&lt;code&gt;/service&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;scope&lt;/code&gt;&lt;/strong&gt;：集群名称。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;a id="log_settings"&gt;&lt;/a&gt;&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="日志"&gt;日志&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;type&lt;/code&gt;&lt;/strong&gt;：设置日志格式，可选 &lt;strong&gt;&lt;code&gt;plain&lt;/code&gt;&lt;/strong&gt; 或 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt;。使用 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt; 格式时，必须安装 &lt;a href="https://pigsty.cc/docs/patroni/installation#extras"&gt;&lt;code&gt;jsonlogger&lt;/code&gt;&lt;/a&gt;。默认值为 &lt;strong&gt;&lt;code&gt;plain&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;level&lt;/code&gt;&lt;/strong&gt;：设置通用日志级别，默认值为 &lt;strong&gt;&lt;code&gt;INFO&lt;/code&gt;&lt;/strong&gt;（参见 &lt;a href="https://docs.python.org/3.6/library/logging.html#levels"&gt;Python logging 文档&lt;/a&gt;）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;traceback_level&lt;/code&gt;&lt;/strong&gt;：设置显示调用栈跟踪的日志级别，默认值为 &lt;strong&gt;&lt;code&gt;ERROR&lt;/code&gt;&lt;/strong&gt;。若希望仅在 &lt;strong&gt;&lt;code&gt;log.level=DEBUG&lt;/code&gt;&lt;/strong&gt; 时才输出调用栈，可将其设为 &lt;strong&gt;&lt;code&gt;DEBUG&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;format&lt;/code&gt;&lt;/strong&gt;：设置日志格式化字符串。若日志类型为 &lt;strong&gt;&lt;code&gt;plain&lt;/code&gt;&lt;/strong&gt;，格式应为字符串，可用属性参见 &lt;a href="https://docs.python.org/3.6/library/logging.html#logrecord-attributes"&gt;LogRecord 属性&lt;/a&gt;。若日志类型为 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt;，格式可以是字符串，也可以是列表——列表中每项对应一个 LogRecord 属性名，只需填写字段名，无需加 &lt;strong&gt;&lt;code&gt;%(&lt;/code&gt;&lt;/strong&gt; 和 &lt;strong&gt;&lt;code&gt;)&lt;/code&gt;&lt;/strong&gt;。若希望以不同键名输出某个日志字段，可使用字典：键为日志字段名，值为期望在日志中显示的名称。默认值为 &lt;strong&gt;%(asctime)s %(levelname)s: %(message)s&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;dateformat&lt;/code&gt;&lt;/strong&gt;：设置日期时间格式化字符串（参见 &lt;a href="https://docs.python.org/3.6/library/logging.html#logging.Formatter.formatTime"&gt;formatTime() 文档&lt;/a&gt;）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;static_fields&lt;/code&gt;&lt;/strong&gt;：向日志中添加额外字段，仅在日志类型设为 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt; 时可用。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;max_queue_size&lt;/code&gt;&lt;/strong&gt;：Patroni 采用两阶段日志记录——日志记录先写入内存队列，由独立线程从队列取出后写入 stderr 或文件。内部队列默认最多保存 &lt;strong&gt;&lt;code&gt;1000&lt;/code&gt;&lt;/strong&gt; 条记录，足以覆盖过去约 1 小时 20 分钟的日志。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;dir&lt;/code&gt;&lt;/strong&gt;：应用日志的写入目录，该目录必须存在且对运行 Patroni 的用户可写。设置此值后，默认保留 4 个 25MB 的日志文件，可通过 &lt;strong&gt;&lt;code&gt;file_num&lt;/code&gt;&lt;/strong&gt; 和 &lt;strong&gt;&lt;code&gt;file_size&lt;/code&gt;&lt;/strong&gt;（见下文）调整保留策略。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;mode&lt;/code&gt;&lt;/strong&gt;：日志文件的权限（例如 &lt;strong&gt;&lt;code&gt;0644&lt;/code&gt;&lt;/strong&gt;）。若未指定，权限将根据当前 umask 值设置。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;file_num&lt;/code&gt;&lt;/strong&gt;：保留的应用日志文件数量。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;file_size&lt;/code&gt;&lt;/strong&gt;：触发日志滚动的 patroni.log 文件大小（单位：字节）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;loggers&lt;/code&gt;&lt;/strong&gt;：本节允许按 Python 模块重新定义日志级别：
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;patroni.postmaster: WARNING&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;urllib3: DEBUG&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;deduplicate_heartbeat_logs&lt;/code&gt;&lt;/strong&gt;：若设为 &lt;strong&gt;&lt;code&gt;true&lt;/code&gt;&lt;/strong&gt;，连续相同的心跳日志将不再重复输出，默认值为 &lt;strong&gt;&lt;code&gt;false&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;div class="td-alert td-alert--md alert alert-warning" role="alert"&gt;&lt;div class="td-alert-heading alert-heading" role="heading"&gt;警告&lt;/div&gt;
 &lt;div class="td-alert-body"&gt;
 &lt;p&gt;HA 循环的执行时间是诊断因资源耗尽等问题导致故障转移的重要信息。当 &lt;strong&gt;&lt;code&gt;deduplicate_heartbeat_logs&lt;/code&gt;&lt;/strong&gt; 设为 &lt;strong&gt;&lt;code&gt;true&lt;/code&gt;&lt;/strong&gt; 时，HA 循环的执行日志将不再生成（除非主库发生变更），因此这部分潜在有用的信息将无法从日志中获取。&lt;/p&gt;</description></item><item><title>环境变量</title><link>https://pigsty.cc/docs/patroni/config/env/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pigsty.cc/docs/patroni/config/env/</guid><description>&lt;blockquote&gt;
&lt;p&gt;原始页面： &lt;a href="https://patroni.readthedocs.io/en/latest/ENVIRONMENT.html"&gt;https://patroni.readthedocs.io/en/latest/ENVIRONMENT.html&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;a id="env"&gt;&lt;/a&gt;
可以使用系统环境变量覆盖 Patroni 配置文件中定义的部分配置参数。本文档列出了 Patroni 处理的所有环境变量。通过环境变量设置的值始终优先于配置文件中的值。&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="全局通用"&gt;全局/通用&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_CONFIGURATION&lt;/code&gt;&lt;/strong&gt;：可通过 &lt;a href="https://pigsty.cc/docs/patroni/config#config"&gt;&lt;code&gt;PATRONI_CONFIGURATION&lt;/code&gt;&lt;/a&gt; 环境变量为 Patroni 提供完整配置。设置此变量后，其他所有环境变量将被忽略！&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_NAME&lt;/code&gt;&lt;/strong&gt;：运行当前 Patroni 实例的节点名称，在集群内必须唯一。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_NAMESPACE&lt;/code&gt;&lt;/strong&gt;：Patroni 在配置存储中保存集群信息的路径。默认值：&amp;quot;/service&amp;quot;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_SCOPE&lt;/code&gt;&lt;/strong&gt;：集群名称。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id="日志"&gt;日志&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_TYPE&lt;/code&gt;&lt;/strong&gt;：日志格式，可为 &lt;strong&gt;&lt;code&gt;plain&lt;/code&gt;&lt;/strong&gt; 或 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt;。使用 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt; 格式需要安装 &lt;a href="https://pigsty.cc/docs/patroni/installation#extras"&gt;&lt;code&gt;jsonlogger&lt;/code&gt;&lt;/a&gt;。默认值为 &lt;strong&gt;&lt;code&gt;plain&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_LEVEL&lt;/code&gt;&lt;/strong&gt;：全局日志级别。默认值为 &lt;strong&gt;&lt;code&gt;INFO&lt;/code&gt;&lt;/strong&gt;（参见 &lt;a href="https://docs.python.org/3.6/library/logging.html#levels"&gt;Python logging 文档&lt;/a&gt;）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_TRACEBACK_LEVEL&lt;/code&gt;&lt;/strong&gt;：显示 traceback 的日志级别。默认值为 &lt;strong&gt;&lt;code&gt;ERROR&lt;/code&gt;&lt;/strong&gt;。若希望仅在 &lt;strong&gt;&lt;code&gt;PATRONI_LOG_LEVEL=DEBUG&lt;/code&gt;&lt;/strong&gt; 时才显示 traceback，可将其设置为 &lt;strong&gt;&lt;code&gt;DEBUG&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_FORMAT&lt;/code&gt;&lt;/strong&gt;：日志格式化字符串。若日志类型为 &lt;strong&gt;&lt;code&gt;plain&lt;/code&gt;&lt;/strong&gt;，格式应为字符串，可用属性参见 &lt;a href="https://docs.python.org/3.6/library/logging.html#logrecord-attributes"&gt;LogRecord 属性文档&lt;/a&gt;。若日志类型为 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt;，格式除字符串外还可以是列表，每个列表项对应一个 LogRecord 属性，只需填写字段名，省略 &lt;strong&gt;&lt;code&gt;%(&lt;/code&gt;&lt;/strong&gt; 和 &lt;strong&gt;&lt;code&gt;)&lt;/code&gt;&lt;/strong&gt;。若希望以不同键名输出某个字段，可使用字典，键为日志字段名，值为希望在日志中显示的字段名。默认值为 &lt;strong&gt;%(asctime)s %(levelname)s: %(message)s&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_DATEFORMAT&lt;/code&gt;&lt;/strong&gt;：日期时间格式化字符串（参见 &lt;a href="https://docs.python.org/3.6/library/logging.html#logging.Formatter.formatTime"&gt;formatTime() 文档&lt;/a&gt;）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_STATIC_FIELDS&lt;/code&gt;&lt;/strong&gt;：为日志添加额外字段，仅在日志类型为 &lt;strong&gt;&lt;code&gt;json&lt;/code&gt;&lt;/strong&gt; 时可用。示例：&lt;strong&gt;&lt;code&gt;PATRONI_LOG_STATIC_FIELDS=&amp;quot;{app: patroni}&amp;quot;&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_MAX_QUEUE_SIZE&lt;/code&gt;&lt;/strong&gt;：Patroni 采用两步日志记录机制。日志记录先写入内存队列，由独立线程从队列中取出后写入 stderr 或文件。内存队列容量默认限制为 &lt;strong&gt;&lt;code&gt;1000&lt;/code&gt;&lt;/strong&gt; 条，足以保留过去约 1 小时 20 分钟的日志。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_DIR&lt;/code&gt;&lt;/strong&gt;：应用程序日志的写入目录。该目录必须已存在且对运行 Patroni 的用户可写。设置此变量后，默认保留 4 个 25MB 的日志文件，可通过 &lt;strong&gt;&lt;code&gt;PATRONI_LOG_FILE_NUM&lt;/code&gt;&lt;/strong&gt; 和 &lt;strong&gt;&lt;code&gt;PATRONI_LOG_FILE_SIZE&lt;/code&gt;&lt;/strong&gt; 调整（见下文）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_MODE&lt;/code&gt;&lt;/strong&gt;：日志文件权限（例如 &lt;strong&gt;&lt;code&gt;0644&lt;/code&gt;&lt;/strong&gt;）。若未指定，将基于当前 umask 值设置。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_FILE_NUM&lt;/code&gt;&lt;/strong&gt;：保留的应用程序日志文件数量。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_FILE_SIZE&lt;/code&gt;&lt;/strong&gt;：触发日志滚动的 patroni.log 文件大小（字节）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_LOGGERS&lt;/code&gt;&lt;/strong&gt;：按 Python 模块重新定义日志级别。示例：&lt;strong&gt;&lt;code&gt;PATRONI_LOG_LOGGERS=&amp;quot;{patroni.postmaster: WARNING, urllib3: DEBUG}&amp;quot;&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;PATRONI_LOG_DEDUPLICATE_HEARTBEAT_LOGS&lt;/code&gt;&lt;/strong&gt;：设置为 &lt;strong&gt;&lt;code&gt;true&lt;/code&gt;&lt;/strong&gt; 时，连续且内容相同的心跳日志不再重复输出。默认值为 &lt;strong&gt;&lt;code&gt;false&lt;/code&gt;&lt;/strong&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;div class="td-alert td-alert--md alert alert-warning" role="alert"&gt;&lt;div class="td-alert-heading alert-heading" role="heading"&gt;警告&lt;/div&gt;
 &lt;div class="td-alert-body"&gt;
 &lt;p&gt;HA 循环的执行时间是诊断资源耗尽等问题所致故障转移的重要信息。将 &lt;strong&gt;&lt;code&gt;PATRONI_LOG_DEDUPLICATE_HEARTBEAT_LOGS&lt;/code&gt;&lt;/strong&gt; 设置为 &lt;strong&gt;&lt;code&gt;true&lt;/code&gt;&lt;/strong&gt; 后，HA 循环执行将不产生日志（除非领导者发生变化），导致这些潜在有用的信息无法从日志中获取。&lt;/p&gt;</description></item></channel></rss>